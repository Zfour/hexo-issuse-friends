setting:
  request: # 网络请求设置
    timeout: 10 # 超时设置
    ssl: false # ssl设置

  gitee_friends_links: # gitee友链配置
    enable: true
    owner: xaoxuu
    repo: friends
    state: open
    labelid: '77925545'
    # 当为none时不筛选label

  github_friends_links: # github友链配置
    enable: true
    owner: xaoxuu
    repo: friends
    state: open
    label: 'active' 
    # 当为none时不筛选label
